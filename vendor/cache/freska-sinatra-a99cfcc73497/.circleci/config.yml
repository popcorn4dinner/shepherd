version: 2
jobs:
  test:
    docker:
      - image: popcorn4dinner/ruby-build
        environment:
          RACK_ENV: test
          APP_ENV: test
    steps:
      - add_ssh_keys:
          fingerprints:
            - "24:cd:cd:33:6d:7f:c6:7a:77:29:bc:73:8b:5f:1b:2b"
      - checkout
      - run:
          name: Install gems
          command: bundle install
      - run:
          name: Run tests
          command: bundle exec rspec

workflows:
  version: 2
  run_tests:
    jobs:
      - test
